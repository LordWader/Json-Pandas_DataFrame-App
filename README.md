# Json-Pandas_DataFrame-App
A simple web service with a REST API, UI and a database.
To work correctly, the program requires installed Python 3.x, as well as the following libraries: Flask, Cherrypy, Pandas
